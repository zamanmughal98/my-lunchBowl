import { Strings } from '../../common/strings';
import { SliderTextContainer } from '../../css';

export const ImageSLiderText = () => {
  return (
    <SliderTextContainer>
      {Strings.internationalFlavorsDelivered}
    </SliderTextContainer>
  );
};
